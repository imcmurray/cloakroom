{
  "//": "Cloakroom GUARD profile for Claude Code. Robust, no decoy-reasoning.",
  "//1": "Blocks reading a file full of PII/secrets into the model's context.",
  "//2": "Copy the `hooks` block into .claude/settings.json (project) or ~/.claude/settings.json (global).",
  "//3": "Requires `cloak` on PATH (npm link) or replace with an absolute path to dist-cli/cloak.mjs.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [{ "type": "command", "command": "cloak hook guard" }]
      }
    ]
  }
}
